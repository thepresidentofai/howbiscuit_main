---
const { title = 'Cheap and safe path' } = Astro.props;
---

<section class="hb-box" data-variant="cheap-safe" aria-labelledby="cheap-safe-title">
  <h2 id="cheap-safe-title" class="hb-box-label">{title}</h2>
  <slot />
</section>
